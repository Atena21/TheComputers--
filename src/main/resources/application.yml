server:
  port: 8080

spring:
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .html

  datasource:
    url: "jdbc:postgresql://localhost:5432/postgres?currentSchema=chat"
    username: postgres
    password: ***mudar***

  jpa:
    database: postgresql
    show-sql: true
    generate-ddl: false
    hibernate:
      ddl-auto: update
